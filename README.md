# База знаний по вселенной Хаяо Миядзаки

Веб‑приложение‑база знаний, посвящённая фильмам Хаяо Миядзаки и студии Ghibli.  
Проект реализован как учебный курсовой: нормализованная PostgreSQL‑БД, сервер на Node.js/Express и аккуратный фронтенд на чистых HTML, CSS и JavaScript.

---

## ✨ Возможности

- **Каталог произведений**
  - Список всех фильмов/произведений с постерами, рейтингами и возрастными ограничениями.
  - Фильтрация по году выхода и жанрам, текстовый поиск по названию.

- **Страница фильма**
  - Подробное описание фильма: постер, названия, год, тип, продолжительность, возрастной рейтинг, синопсис.
  - Перечень жанров, участников съёмочной группы (режиссёр, сценарист, композитор и др.), связанных персонажей и наград.

- **Интеллектуальный поиск**
  - Поиск по свободным текстовым запросам («фильмы про летающие замки», «самые мрачные работы» и т.п.).
  - Результаты выводятся в знакомом виде карточек фильмов.

- **Таймлайн**
  - Отдельная страница‑хронология с визуализацией фильмов по годам выхода.
  - Быстрые переходы с таймлайна к карточкам фильмов.

- **Пользовательский профиль**
  - Авторизация / выход из системы.
  - Оценки фильмов и отметки «понравилось».
  - Просмотр собственных списков просмотренного и избранного.

- **Админ‑панель**
  - Управление фильмами, персонами, персонажами, жанрами и наградами.
  - Формы создания/редактирования с базовой валидацией.
  - Работа только для пользователей с ролью администратора.

---

## 🧱 Технологии

**Backend**

- Node.js
- Express
- PostgreSQL
- `pg` (подключение к БД)
- Сессии / авторизация, разграничение прав (пользователь / администратор)

**Frontend**

- HTML5
- CSS3 (flexbox, grid, адаптивная вёрстка)
- JavaScript (ES6+, модульная структура)
- Небольшие анимации и плавные эффекты

## ⚙️ Установка и запуск локально

### 1. Предварительные требования

- Node.js 18+ (актуальная LTS‑версия)
- PostgreSQL 14+ (или совместимая)
- Git (если клонируете из GitHub)

### 2. Клонирование репозитория

```bash
git clone [https://github.com/filage/Hayao-Miyazaki-Knowledge-Base.git](https://github.com/filage/Hayao-Miyazaki-Knowledge-Base.git)
cd Hayao-Miyazaki-Knowledge-Base
```

### 3. Установка зависимостей

```bash
npm install
```

### 4. Создание базы данных

В PostgreSQL создайте отдельную базу, например:

```sql
CREATE DATABASE miyazaki_db;
```

Накатите структуру и начальные данные:

```bash
psql -U <username> -d miyazaki_db -f db/schema.sql
psql -U <username> -d miyazaki_db -f db/seed.sql
```

Где `<username>` — ваш пользователь PostgreSQL.

### 5. Настройка переменных окружения

В корне проекта создайте файл `.env` (он игнорируется Git и не попадает в репозиторий).

Пример содержимого:

```env
DATABASE_URL=postgres://user:password@localhost:5432/miyazaki_db
PORT=3000
SESSION_SECRET=super-secret-string
```

- `DATABASE_URL` — строка подключения к PostgreSQL.  
- `PORT` — порт HTTP‑сервера (по умолчанию 3000).  
- `SESSION_SECRET` — секрет для подписания сессий (любая достаточно длинная строка).

### 6. Запуск приложения

```bash
npm start
```

После успешного запуска:

- Главная страница: <http://localhost:3000/>
- Страница фильма: `http://localhost:3000/film.html?id=<uuid>`
- Таймлайн: <http://localhost:3000/timeline.html>
- Профиль: <http://localhost:3000/profile.html>
- Админ‑панель: <http://localhost:3000/admin.html>

---

## 👤 Работа с пользователями и правами

Приложение поддерживает:

- **Гостевой режим** — просмотр базы знаний без авторизации.
- **Зарегистрированный пользователь** может:
  - добавлять фильмы в свои списки;
  - ставить оценки;
  - формировать личную подборку «понравилось».
- **Администратор** дополнительно имеет доступ к:
  - админ‑панели;
  - созданию, редактированию и удалению записей в БД через веб‑интерфейс.

Учётные записи создаются через форму регистрации или заранее через SQL‑скрипты.

---

## 🧪 Ручное тестирование

Для проверки основных сценариев:

### Главная страница

- Убедиться, что список произведений загружается без ошибок.
- Проверить фильтрацию по году и жанрам.
- Проверить текстовый поиск по названию.

### Интеллектуальный поиск

- Ввести несколько разных запросов (по теме, персонажам, настроению).
- Оценить релевантность результатов и поведение при пустых / странных запросах.

### Таймлайн

- Проверить корректное распределение фильмов по годам.
- Убедиться, что переходы к страницам фильмов работают корректно.

### Страница фильма

- Для неавторизованного пользователя: проверка только чтения.
- Для авторизованного: проверка установки/изменения оценки и добавления в списки.

### Профиль

- Убедиться, что в профиле отражаются выставленные рейтинги и отметки «понравилось».

### Админ‑панель

- Авторизоваться под администратором.
- Создать новый фильм, отредактировать существующий, удалить тестовый.
- Убедиться, что изменения видны на клиентских страницах.

---

## 🔭 Возможные направления развития

**Расширение набора данных**

- Полная фильмография.
- Дополнительные факты, цитаты, тематические подборки.

**Улучшение UX**

- Более продвинутый адаптивный дизайн для мобильных устройств.
- Расширенные фильтры и сортировки на клиенте.

**Развитие ИИ‑поиска**

- Поддержка многоязычных запросов.
- Объяснение причин рекомендаций.
- Диалоговый режим общения с системой.

**Техническое развитие**

- Кэширование часто запрашиваемых данных.
- Оптимизация запросов к БД и индексов.
- Контейнеризация и развёртывание в облаке.

**Безопасность**

- Более строгие требования к паролям.
- Защита от типичных веб‑атак.
- Двухфакторная аутентификация для админов.